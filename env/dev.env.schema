# Shared dev environment for all Underlay consumer apps: running inside the
# effigy container dev stack. Enables dev seeds, CORS origin mirroring, and
# dev-only tooling. Single identifier fleet-wide — secrets come from the
# effigy vault, not from this file.
ENVIRONMENT=effigy
